function calculateNumbers(var 1, var 2) {
    var sum = var 1 + var 2;
    var difference = var 1 - var 2;
    var product = var 1 * var 2;
    var quotient = var 1 / var 2;

    return {
        sum: sum,
        difference: difference,
        product: product,
        quotient: quotient
    };
}